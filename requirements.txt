numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
tabulate>=0.9

# Optional. The pipeline falls back to scikit-learn's histogram booster and
# skips the MCMC cross-check if these are absent, and reports which path it took.
# lightgbm>=4.0
# pymc>=5.10
